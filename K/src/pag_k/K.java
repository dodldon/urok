package pag_k;
/**Конструктор,
 * инициализирует объект при создании,
 * конструктор по умолчанию инициализирует все переменные экземпляра: 
 * синтаксисит ...=new имя класса();
*/

public class K {

    int i;       //null
    int x;
    boolean b;   //false
//в классе К есть конструктор по умолчанию 
//передаётся параметр переменной экземпляра
   
}
